/** An authorization code obtained from the Strava OAuth2 flow. */
export type Oauth2Code = string;
/** Options for the OAuth2 flow. */
export type OAuth2Opts = { redirectUri: Url };
/** A URL string. */
export type Url = string;
