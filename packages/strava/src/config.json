{
  "description": "Bootstrap configuration",
  "settings": {
    "clientAppFile": "${HOME}/.strava/clientapp.secrets.json",
    "segmentsFile": "${HOME}/.strava/segments.json",
    "userSettingsFile": "${HOME}/.strava/user.settings.json",
    "credentialsFile": "${HOME}/.strava/credentials.json"
  }
}
