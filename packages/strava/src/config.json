{
  "description": "Bootstrap configuration",
  "paths": {
    "clientCreds": "${HOME}/.strava/client.creds.json",
    "userSegments": "${HOME}/.strava/user.segments.json",
    "userSettings": "${HOME}/.strava/user.settings.json",
    "userCreds": "${HOME}/.strava/user.creds.json"
  }
}
